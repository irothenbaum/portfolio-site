<div class="breadcrumbs">
    <span class="path-item">404jkfoundit.com</span>
    <span class="path-item">portfolio</span>
    {% for b in crumbs %}
        <span class="path-item">{{ b }}</span>
    {% endfor %}
</div>
